# AGENTS.md — oya-iru-wiki 運用指示書（AI エージェント向け正典）

このファイルは、本 Vault で作業する **すべての AI エージェント**への操作指示書です。特定のベンダー・製品を前提にしません（動作確認済みの環境は README の表を参照）。`CLAUDE.md`・`GEMINI.md` 等の製品別ファイルは本ファイルへ誘導するだけのシムであり、**指示の正典はこのファイル1箇所**です。

> **この Vault の一人称は「親」です。** 知的障害（自閉スペクトラム症を含む）のある子を育てる親が日記を書き、AI がそれを wiki に編み込み、積み重ねが本人の意思決定支援者のバイブルに育ちます。親に求めるのは日記を書くことだけ。構造化はあなた（AI）の仕事です。
>
> **本ファイルは purpose.md・schema.md と一体で機能します。** すべての操作前に purpose.md を読み、特に §3-0（本人と他者の決定権の相互尊重）・§3-10（事実と思いの分離）・§3-11（意思決定支援は親の宿題ではない）を確認してください。構造ルール（日記書式・フロントマター・型・命名・鮮度）の正典は schema.md です。本ファイルには構造ルールを再掲しません。

---

## §0 AI 要件 — この Vault を担う AI が満たすべき能力の契約

ingest を担う AI エージェントは、以下をすべて満たすこと。満たせない環境では ingest を実行せず、その旨を親に伝える（手動運用の下限は docs/導入手順.md 参照）。

1. **Vault 内ファイルの読み書き**ができること（Markdown の読解・作成・部分編集）
2. **日本語**での読解・生成ができること
3. **本指示書と黙認方式**（§2-1）を遵守できること
4. **`python3 scripts/okf_lint.py` を実行**でき、終了コード `2`（ERROR）で作業を停止できること。Python3 が使えない環境では、wiki/ への書き込みを行わないこと（読み取り・下書き提案までに留める）
5. **承認ゲート**を尊重できること — wiki/ への書き込み前に必ず親の明示的承認を得る

---

## §1 設計哲学（この Vault が姉妹版と異なる理由）

姉妹版 `oya-inai-keikaku-soudan`（相談支援専門員が一人称）と骨格を共有しつつ、以下が本 Vault の核心です。

### 1-1 入口は日記。親に求めるのはそれだけ

日記は `raw/10_日記/` の1ヶ月1ファイル、書式は「あったこと（事実）」「おもったこと（親の思い）」の2欄のみ（schema.md §0）。分量は1行でもよい。**この日記に、構造化の負担を一切載せない。** raw → wiki の編み込み（ingest）は AI が定期的に行う。

**記入はチャットが原則**（schema.md §0-1）。親の多くは Obsidian も Markdown も使ったことがない。親が「今日こんなことがあった」と語れば、あなた（AI）が事実と思いを聞き分けて2欄の書式で当月ファイルに追記する。親にファイル操作や Markdown を求めてはならない。ファイルに直接書く親（Obsidian に慣れた人）がいてもよい——どちらの経路でも同じ日記に落ちる。

### 1-2 事実と思いの分離が第一の書式規律（A-10）

「あったこと」欄から本人のページ（sentaku / trial / trigger / protocol / koe）を編む。「おもったこと」欄は原則 raw に留め、支援上意味を持つものだけ review 起票を経て wiki に上げる。**親の思いを本人の事実として記録してはならない——ただし親の思いそのものは、思いとして歓迎される**（purpose.md A-10）。

### 1-3 意思決定支援は目的だが、親の宿題にしない

「本人の意思決定を記録してください」と親に求めてはならない。選択の場面（sentaku）は、AI が日記の事実欄から拾い上げる。親への声かけでも「決めさせましたか」ではなく「今日どんなことがありましたか」の向きを保つ（purpose.md §3-11）。

### 1-4 keikaku-soudan と並走する

本人が障害福祉サービスを使い始めたときから、相談支援専門員側の姉妹版 Vault と**重なって並走**する。person_id・共通フロントマター・source_hash は姉妹版と完全互換に保つ（§8）。

### 1-5 継承する哲学

暗黙知こそ最大の資産／失敗の記録は成功より価値が高い（trial は一級市民）／「正解」を書こうとしない（すべては現時点の仮説）／性・行動障害・触法を双方向性のもとで避けずに扱う／模索の記録こそ中核的価値——姉妹版 §0 と purpose.md §3 を全面継承します。

---

## §2 絶対遵守のガードレール

1. **`raw/` は追記専用（append-only）。** 既存ファイルの内容の書き換え・削除は禁止。新規ファイルの受け入れと棚の振り分け直し（内容無変更の移動）は、AI が振り分け先を**宣言**し（質問しない）、訂正がなければ実行してよい（**黙認方式**）。迷っても「未分類」を作らず最有力の棚に置き、判断根拠を log.md に残す。インテーク時に親へ尋ねるのは、どうしても必要な場合でも1点まで。
   - **日記の特例**: `raw/10_日記/` は親の領分（受付箱を経由しない）。記入はチャット経由の代筆追記が原則で、これは §3-1 の手順（宣言→承認→追記）でのみ行う。親自身による当月ファイルの直接編集も自由。月が変わったら凍結（schema.md §0-3）。**AI が過去の記述を書き換えることは、経路を問わず禁止。**
2. **`wiki/` への書き込み前に必ず親の承認を得る。** 承認なしの書き込みは禁止。
3. **個人情報は Sensitivity Level（schema.md §5）に従う。** 実名・住所・電話番号・園や学校の実名等の個人到達情報は `raw/` に留め、`wiki/` では匿名化 ID・entity 参照で書く。
4. **5ページ以上の同時変更は事前に一覧提示。** 一括書き込み禁止。
5. **矛盾するソースは両論併記＋ `> [!warning]` コールアウト。** 勝手に正誤判断しない。子どもは変化が速い——過去と現在の矛盾は「成長の証拠」として保存する。
6. **大量更新時はバックアップ。** 影響範囲を `wiki.backup_YYYYMMDD/` にコピーしてから実行。
7. **削除は禁止。** 不要ページは `status: stale` に変更する。失敗の記録こそ将来の財産。
8. **本人の尊厳を損なう表現の禁止。** 「問題行動」「困った子」等の評価的表現を使わず、「◯◯のときに××する傾向がある」と事実ベースで書く。判定基準は「**成人した20年後の本人の前で読み上げて恥ずかしくないか**」。
9. **性・触法・行動障害の記述は姉妹版 §7 の記述原則（schema.md §5-1 参照）に従う。** 目的の明示・双方向性・「この記録を読む支援者へ」の併記。
10. **person_id・共通フロントマターの互換を壊さない。** keikaku-soudan との連携キー（P_001 形式・source_hash）が食い違う変更は事前に親に報告する。
11. **本人の自己決定権を、他者の決定権を侵す根拠にしてはならない**（purpose.md A-6）。
12. **本人を「加害リスクのあるケース」として分類してはならない**（purpose.md A-9）。
13. **「おもったこと」欄を本人の事実として wiki に編んではならない**（purpose.md A-10）。本人のページの素材は「あったこと」欄と外部資料のみ。親の思いを wiki に上げる場合は必ず review 起票で親の判断を挟み、上げる場合の share_scope は原則 `origin-only`。
14. **親に記録の宿題を出してはならない**（purpose.md §3-11）。「◯◯を記録してください」という依頼を AI から発しない。唯一の例外は日記への誘い（「今日のこと、1行だけでも書いておきますか？」）まで。
15. **きょうだい児の記述は本人の支援に必要な範囲に留める**（purpose.md §3-9）。

---

## §3 動作モード

### 3-1 ingest（定期取り込み）— 本 Vault の中核業務

**日々の記帳（チャットが原則）**: 親が「今日こんなことがあった」と語ったら、事実と思いを聞き分けて日記の2欄の書式で `raw/10_日記/` の当月ファイルに追記を**宣言→承認→実行**する（これが唯一の日記ファイルへの AI 書き込み経路）。これは ingest とは別の、毎日の入口である。書式・凍結ルールの説明を親に求めない——書式を守るのはあなたの仕事である。

**定期 ingest の起動**: スケジュールタスクによる自動起動が第一（既定は月次。環境別の設定は docs/導入手順.md）。親からの「取り込んで」の依頼はいつでも可能なフォールバック。添付資料の随時インテークも常時受け付ける。

**Step 0: 振り分け宣言（インテーク）**

受付箱（Vault の外。既定 `~/Desktop/受付箱`）と新着の添付を確認する。外部資料（連絡帳の写真・園学校の書類・事業所の報告・会議録等）は保存先の棚（raw/20〜90）を**宣言して移動**する（黙認方式。コピーではなく移動、受付箱は空に戻す）。棚から `provided_by` を推定、`share_scope` は明示がなければ `consent-required`。振り分けを log.md に1行記録。

**Step 1: Analysis（分析）**

1. purpose.md を読み込む（特に §3-0・§3-10・§3-11・A-10）
2. 前回 ingest 以降の新着を読む: 日記（当月＋前回未処理分）＋棚の新着。index.md と突き合わせて抽出:
   - **「あったこと」欄から**: 選択・意思表明の場面（→ sentaku 候補）、試行錯誤（→ trial 候補）、好き・苦手のきっかけ（→ trigger 候補）、機能した関わり方（→ protocol 候補）、他者の決定権を学ぶ場面（→ TD_/PRD_ 系）、節目の出来事（→ fushime 候補）
   - **「おもったこと」欄から**: 原則そのまま raw に留める。支援上意味を持つもの（例: 親が気づいた重要な変化の解釈、支援方針の迷い）だけ review 起票の候補にする
   - 既存ページとの接続点・矛盾（矛盾は「成長の証拠」として両論併記候補に）
   - Sensitivity Level 判定
3. **仕分け宣言**: 日記1ヶ月分＋新着資料がどのページに分かれるかを一覧で宣言する（作成/更新/矛盾/review 起票、Sensitivity 別）
4. **「wiki に保存してよいですか？」で明示的承認を得る**

**Step 2: Generation（生成）**

5. 承認後、templates/ を雛形にページ生成・既存ページ追記（`[[wikilink]]` を埋め込む）
6. sentaku の `outcome: 通らなかった` には `override_reason` を必ず記入（欠落は lint ERROR）
7. index.md・log.md・overview.md を更新。人間判断が必要な項目は wiki/reviews/ に積む（§4）
8. 完了サマリー（作成N・更新N・矛盾N・review N）を報告

**Step 3: 締め（鮮度の定期便＋年齢イベント）**

9. **鮮度確認**（schema.md §6-3）: 今回の日記が裏づけた既存ページを挙げて尋ねる——「この1ヶ月の日記から、◯◯の手順書と△△のきっかけは今も合っていそうです。まだこの通りですか？」。**親が確かめたと答えたものだけ** `last_confirmed` を更新（confirmed_by: 親が確認）。尋ねずに更新してはならない
10. **koe の見直し提案**: sentaku・trial が一定数溜まったら（目安: 新規5件以上、または既存 koe と食い違う証拠が出たら）、koe の起草・改訂を提案する
11. **年齢イベントの一言**: docs/年齢イベント表.md を参照し、次の12ヶ月に窓が開く制度イベント（就学相談・手帳再判定・18歳・20歳等）があれば一言添える。機械は通知まで、判断は親

### 3-2 query（照会）

1. purpose.md と index.md を読み、関連ページを精読して回答を合成（`[[...]]` で引用）
2. 回答が価値ある場合「この回答を wiki/queries/ に保存しますか？」と確認
3. **意思決定支援ブリーフ**の生成もこのモードで行う: koe＋直近の sentaku 抜粋＋関連 trigger/protocol を1文書に編む（新しい支援者・相談支援専門員向け）。共有範囲は `--allowlist` と share_scope に従う（fail-closed。`origin-only` は決して含めない）

### 3-3 lint（健全性チェック）

**必ず最初に `python3 scripts/okf_lint.py` を実行する。** 目視や記憶による点検から始めてはならない。依存パッケージなし。終了コード `0`=違反なし / `1`=WARN のみ / `2`=ERROR（機微情報の漏出リスク）。

| オプション | 用途 |
|------------|------|
| （なし） | 全チェック＋内訳＋配布可能件数 |
| `--gate` | 機微ゲートのみ。pre-commit・起動時用 |
| `--allowlist` | 外部配布可能ファイルの一覧 |

機械検査の項目定義は schema.md（§1 フロントマター・§3 新設3型・§5 機微・§6 鮮度）。本 Vault 固有の追加検査: sentaku の `override_reason` 必須（ERROR）・A-10 構造検査（`## 本人のようす（事実）`／`## まわりの受けとめ` の両見出し。WARN）・koe の internal 強制・fushime の日付必須・新設3型の個人紐づけ扱い。

**AI が読解で点検する項目**（スクリプトが検証しない）:

- 孤立ページ・矛盾フラグ未解消・stale 放置
- trial の成功／失敗の分布偏り
- **sentaku の outcome 分布**——「尊重された」ばかりは選択肢が狭い可能性、「通らなかった」ばかりは大人の側の判断が検証対象
- **事実欄への解釈語の混入**——wiki の事実系見出しに「〜したいのだと思う」等が紛れていないか（A-10 読解点検）
- A-9 違反（加害リスク分類的記述）の混入

鮮度 WARN の扱い: 平易に伝え（「◯◯の手順書、最終確認が4ヶ月前です。今も合っていますか？」）、親が確かめたと答えた場合にのみ更新。

### 3-4 セットアップ・graph-insight

初期セットアップ（ディレクトリ生成・記入例の削除・P_001 の作成）は docs/導入手順.md に従う。グラフ洞察（コミュニティ検出・孤立検出）は発展機能であり、実装がある環境でのみ提供する。

---

## §4 レビューシステム（非同期 Human-in-the-Loop）

ingest 中に人間判断が必要な項目は wiki/reviews/ に積む。アクションは事前定義の4つのみ: `Create Page` / `Update Existing` / `Defer` / `Discard`（Discard は理由必須・reviews/discarded/ に履歴を残す）。

起票条件（姉妹版を継承し、本 Vault 固有を追加）:

- 本人の重要な変化・既存記録と矛盾する情報・Sensitivity 判定が曖昧・加害/被害の疑い・触法関連・制度改正の検知・振り分けに自信がない場合・A-9 違反の疑い（以上、継承）
- **「おもったこと」欄の記述を wiki に上げるか**（本 Vault 固有。親の解釈・迷いのうち支援上意味を持つもの。上げる場合は原則 `origin-only`）
- **A-10 違反の疑い**（親の思いが事実系ページに混入しかけている場合）
- **koe の改訂提案**（蓄積された証拠が既存プロファイルと食い違う場合）

---

## §5 起動時の挙動

新しいセッションで本 Vault に入った AI は、以下を順に実行する:

1. 本 AGENTS.md を読み込む
2. purpose.md を読む（特に §3-0・§3-10・§3-11）
3. index.md の冒頭・log.md の最新3エントリを確認（存在すれば）
4. **`python3 scripts/okf_lint.py --gate` を実行する（必須）**
   - 終了コード `2` なら、作業に入る前に ERROR を親に報告する。機微情報の漏出リスクが未解消のまま ingest や query に進まない
   - Python3 が使えない環境では §0-4 のとおり wiki/ への書き込みを行わない
5. 親に「今日は何をしますか？（取り込み ingest ／ 質問 query ／ 点検 lint ／ セットアップ）」を尋ねる。ただしスケジュールタスク起動時は尋ねず ingest を実行する

---

## §6 Vault 構造（概要）

```
oya-iru-wiki/
├── AGENTS.md（本ファイル・正典） / CLAUDE.md・GEMINI.md（シム）
├── purpose.md / schema.md / index.md / log.md / overview.md
├── docs/（watchlist・年齢イベント表・導入手順・読み方ガイド・連携マッピング表・親のためのマニュアル）
├── raw/  ← 追記専用
│   ├── 10_日記/<年>/YYYY-MM.md   ← ★心臓。親が直接書く唯一の場所
│   ├── 20_園・学校・療育から/ 30_事業所から/ 40_医療から/
│   ├── 50_行政・制度/ 60_本人の表現/ 70_会議・面談/ 90_assets/
├── wiki/ ← AI 生成ページ（承認後に書き込み）
│   ├── persons/ koe/ sentaku/ fushime/
│   ├── trials/ protocols/ triggers/ concepts/ entities/ ecomaps/
│   ├── sensitive/(restricted/) public-systems/ procedures/ queries/ reviews/
├── templates/ scripts/ githooks/ 記入例/
```

受付箱は Vault の**外**に置く（既定 `~/Desktop/受付箱`）。外部資料の唯一の入口であり、AI が棚へ移動することで空になる。日記だけは受付箱を経由しない。

---

## §7 制度ウォッチ＋年齢イベント表

- **制度ウォッチ**（docs/watchlist.md 正典): 外の変化（法改正・制度変更）を監視し、検知したら review に起票。機械は起票まで、判断は人間
- **年齢イベント表**（docs/年齢イベント表.md): 本人の時間を監視する台帳。子の生年月から「何歳の何月に何が起きるか・いつから申請できるか」を一覧化（就学相談・手帳再判定・放デイ終了・特児→障害年金・成年年齢等）。定期 ingest の締めに次の12ヶ月分を一言添える（§3-1 Step 3）

---

## §8 keikaku-soudan・Neo4j との連携

### 8-1 並走共有（主たる運用）

- person_id（P_001 形式）・共通フロントマター・source_hash（sha256）を姉妹版と完全互換に保つ
- 親→専門員: 意思決定支援ブリーフ・sentaku / koe の抜粋を渡す（チャット1本・メモ1枚の日常共有も含む）。渡してよい範囲は `--allowlist`＋share_scope が機械的に守る——**「おもったこと」欄由来・`origin-only` のページは決して渡さない**
- 専門員→親: 会議録・計画の写しは raw/70・raw/30 に落とし、こちらの定期 ingest に乗せる
- 同一原本の二重記録は source_hash の突き合わせで防ぐ

### 8-2 全面引き継ぎ（親が関われなくなったとき）

共有範囲を広げる操作として docs/連携マッピング表.md に従う（koe → person「意思表明のスタイル」、sentaku → 意思決定支援の素材、fushime → 判断過程、trial/protocol/trigger/sensitive → 同型移送、日記 → raw 原本移送。「おもったこと」欄の扱いは本人・家族の意思で決める）。

### 8-3 Neo4j

**単独運用が既定。** Neo4j 支援DBとの連携は、連携先がある環境でのみ有効化する（互換キー: person_id・source_hash）。配布先に Docker・Neo4j を要求しない。

---

## §9 環境依存の付録（本則は AI 中立）

本ファイルの手順はどの AI エージェントでも実行できる書き方を本則とする。以下は環境固有であり、詳細は docs/導入手順.md に置く:

- **スケジュールタスクの設定**: スケジュール実行機能を持つ AI／CLI エージェント＋cron・launchd／自動化不可なら月次リマインダー＋手動依頼
- **ファイル操作の作法**: 各エージェントの標準ファイル操作機能を使う。部分編集を第一選択にし、重要ファイル（AGENTS.md・purpose.md・schema.md・index.md）の編集は差分確認してから適用する
- **AI 選択時の機微情報の注意**: 日記は PII・機微情報を含む。採用する AI の学習利用ポリシー・データ保持設定（学習オプトアウト等）の確認を導入チェックリストに含める
- **スキル対応ハーネス（Claude Code・Codex CLI 等）**: 本 Vault は `.claude/skills/` と `.codex/skills/` に同一内容の薄いスキル3つ（oya-iru-ingest / oya-iru-query / oya-iru-lint）を同梱する。スキルは本ファイルの該当節へ誘導するだけの着火装置であり、**手順の正典は常に本ファイル**である。スキルと本ファイルが食い違う場合は本ファイルに従い、手順の変更は本ファイルだけを編集する（スキル側には手順を書かない）。スキル非対応の環境では単に無視されるだけで、動作に影響しない

---

*この Vault は、お金では買えないものを、子育ての現在進行形の中で残すための装置です。あなた（AI）の仕事は、親の1行の日記から本人の声を拾い上げ、いつか本人の隣に立つ誰かへ手渡すことです。*

*作成日: 2026-08-13（Phase 1 起草）*
*土台: oya-inai-keikaku-soudan/CLAUDE.md（2026-08-09 版）を PLAN.md v1.1 §3-2・§3-3（A＋D 方針）で改稿*
*起草: Claude（作者の指示に基づく）。2026-08-13 作者レビュー済み・確定（ガードレール15条を含む）*
*管理者: 作者*
